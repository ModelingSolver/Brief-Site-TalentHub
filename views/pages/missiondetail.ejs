
<%- include("../partials/head", { title: "detail de mission" }) %>
<%- include("../partials/header") %> 

<div class="page-container">
  <h1>    </h1>

  <ul>
    <% missions.forEach(mission => { %>
      <li>
        <h2><%= mission.titre %></h2>
        <a href="/detail/<%= mission.id %>">Voir le détail</a>
        <p><%= mission.decription %></p>
        <p>Type: <%= mission.type %></p>
        <p>Date début: <%= mission.date_debut %></p>
        <p>Salaire: <%= mission.salaire_net %> <%= mission.unité_salaire %></p>

        <p>Compétences :</p>
        <ul>
          <% mission.skills.forEach(skill => { %>
            <li><%= skill %></li>
          <% }) %>
        </ul>
      </li>
    <% }) %>
  </ul>
</div>


